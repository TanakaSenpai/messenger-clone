# Supabase configuration (public, safe to embed in client)
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY

# Optional: storage bucket name used by the app (defaults to "conversations")
EXPO_PUBLIC_SUPABASE_BUCKET=conversations
